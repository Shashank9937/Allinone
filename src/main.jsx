import React from "react";
import { createRoot } from "react-dom/client";
import FounderOS from "./components/FounderOS.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FounderOS />
  </React.StrictMode>
);
